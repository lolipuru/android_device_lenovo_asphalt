<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Height of the status bar.
         Do not read this dimen directly. Use {@link SystemBarUtils#getStatusBarHeight} instead.
         -->
    <dimen name="status_bar_height_default">35dp</dimen>

    <!-- Height of the status bar in portrait. The height should be
         Max((status bar content height + waterfall top size), top cutout size) -->
    <dimen name="status_bar_height_portrait">35dp</dimen>
    <dimen name="status_bar_height_landscape">35dp</dimen>

    <!-- Height of a small notification in the status bar -->
    <dimen name="notification_min_height">106.0dip</dimen>

    <!-- Rounded corner settings for secondary built-in display -->
    <dimen name="rounded_corner_radius">26.9dp</dimen>
    <dimen name="rounded_corner_radius_bottom">26.9dp</dimen>
    <dimen name="rounded_corner_radius_top">26.9dp</dimen>

    <!-- Height of area above QQS where battery/time go (equal to status bar) -->
    <dimen name="quick_qs_offset_height">35dp</dimen>
</resources>